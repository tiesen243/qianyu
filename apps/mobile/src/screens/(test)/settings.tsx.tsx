import { Text } from 'react-native'

import { Container } from '@/components/container'

export default function SettingsScreen() {
  return (
    <Container className='px-4'>
      <Text className='text-foreground'>Màn hình cài đặt</Text>
    </Container>
  )
}
